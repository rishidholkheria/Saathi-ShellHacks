import React from 'react'

const SidebarOption = ({ index, text, icon }) => {
    return (
        <div >
            <p className="sidebarOption">{text}</p>
        </div>
    )
}

export default SidebarOption

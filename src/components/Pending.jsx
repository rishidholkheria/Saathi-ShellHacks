import React from 'react'

const Pending = ({ pending }) => {
    return (
        <div className="work">
            <p className="pendingWorkName">{pending}</p>
        </div>
    )
}

export default Pending

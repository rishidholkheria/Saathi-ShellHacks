import React from 'react'

const Verified = ({ verified }) => {
    return (
        <div className="work">

            <p className="pendingWorkName">{verified}</p>
        </div>
    )
}

export default Verified
